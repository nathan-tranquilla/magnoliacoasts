---
export interface Props {
  classes?: string 
}
const { classes, ...props } = Astro.props 
---
<div class={`card flex flex-col py-6 px-4 shadow-card rounded-md bg-white ${classes || ""}`} {...props}>
  <slot />
</div>
---
export interface Props {
  classes?: string 
}
const { classes, ...props } = Astro.props 
---
<div class={`flex flex-col py-6 px-4 shadow-card rounded-md bg-white ${classes || ""}`} {...props}>
  <slot />
</div>
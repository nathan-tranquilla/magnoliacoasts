---
export interface Props {
  title: string;
  classStyles?: string;
}
const { title, classStyles } = Astro.props;
---
<p class={"my-6 font-cormorant-garamond flex justify-center md:text-2xl sm:text-xl text-xl " + classStyles || ""}>{title}</p>
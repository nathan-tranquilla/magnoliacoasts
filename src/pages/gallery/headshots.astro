---
import Gallery, { GalleryType } from "../../layouts/Gallery.astro";

const images = Object.values(
  import.meta.glob(`../../assets/galleries/headshots/*.jpg`, { eager: true }),
).map((module: any) => module.default) as ImageMetadata[];
---

<Gallery
  metaTitle="Mississauga | Headshot Photos"
  description="Headshot photography gallery by Magnolia Coasts in Mississauga. Polished, professional portraits for LinkedIn, acting, and corporate use."
  title="Headshots"
  galleryType={GalleryType.Headshots}
  images={images}
/>
